<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserMapper">
	<resultMap id="userSelectMap" type="com.springjobs.domain.Users">
		<result property="uno" 			column="UNO" 			jdbcType="INTEGER"/>
		<result property="email"	column="UEM" 		jdbcType="VARCHAR" />
		<result property="password" 	column="UPW" 		jdbcType="VARCHAR" />
	</resultMap>
	
	<!-- SQL : INSERT -->
	<insert 	id="addUser"		parameterType="com.springjobs.domain.Users" >
	 	INSERT
		INTO USERS(unm, uem, upw, ucnf) 
		VALUES	 (#{unm},#{uem},#{upw},'0');
	 </insert>
	 
	<select id="login" parameterType="com.springjobs.domain.Users" resultType="com.springjobs.domain.Users">
		SELECT U.UNO,U.UNM,U.UENM,U.UEM,U.UPW,U.UCO,U.USX,U.UCNF,U.UBD,U.UPHO,U.UADR,UURL,URES,C.CNO
		FROM USERS U left outer join CINFOS C
		ON U.uno=C.uno WHERE U.uem=#{uem} and U.upw=#{upw}
    </select>
    
	<select id="idDuplicateCheck" parameterType="com.springjobs.domain.Users" resultType="int">
		SELECT COUNT(*) 
		FROM USERS 
		WHERE uem=#{uem};
    </select>
	 
</mapper>